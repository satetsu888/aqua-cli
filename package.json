{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/cli && npm run build -w packages/stripe-plugin && npm run build -w packages/supabase-plugin && npm run build -w packages/firebase-plugin",
    "test": "npm run test -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  }
}
